// some basic data types in typescript
// some commonly used data types in TypeScript such as number, string, boolean

// String
let myName: string = "waleed hassan";
console.log(myName);     //string Used to represent textual data, enclosed within single or double quotes



// Number
let age: number = 27;
console.log(age);         //Number Used to represent numeric values

// Boolean
let isMarried: Boolean = false;
console.log(isMarried);       //Boolean Used to represent logical values, either true or false

// collectively written
let msg = "hello dear";
let yourAge = 25;
let isStudnet = true;
console.log(msg, yourAge, isStudnet);
